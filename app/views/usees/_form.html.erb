<%= form_with(model: usee) do |form| %>
  <% if usee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(usee.errors.count, "error") %> prohibited this usee from being saved:</h2>

      <ul>
        <% usee.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
